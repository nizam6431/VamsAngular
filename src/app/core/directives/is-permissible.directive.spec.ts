import { IsPermissibleDirective } from './is-permissible.directive';

describe('IsPermissibleDirective', () => {
  it('should create an instance', () => {
    const directive = new IsPermissibleDirective();
    expect(directive).toBeTruthy();
  });
});
